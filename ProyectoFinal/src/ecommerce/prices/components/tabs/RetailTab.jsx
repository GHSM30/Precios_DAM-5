import { Box } from "@mui/material";
import { useState } from "react";

export default function RetailTab() {
    return (
      <Box>
  
            <h2>Tab con la tabla de la coleccion de Lista de Precios</h2>
  
      </Box>
  
    );
  
  }